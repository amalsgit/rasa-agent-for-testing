version: "3.1"

slots:
  code_question:
    type: text
    mappings:
      - type: from_llm

responses:
  utter_ask_code_question:
    - text: "What's your code question?"
      metadata:
        rephrase: True
